ace_worker:
  base_path: /runpod-volume/models
  diffusion_models: diffusion_models/
  text_encoders: text_encoders/
  vae: vae/
  loras: loras/
  # jeankassio's ComfyUI-AceStep_SFT custom node looks for files under
  # models/Audio/ — we keep symlinks there pointing into the standard subdirs
  # so both this custom node AND the canonical ComfyUI ace_step.json workflow
  # see the same on-disk weights without duplication.
  Audio: Audio/
